{
  "name": "ha-timeline-card",
  "version": "0.2.0",
  "description": "Home Assistant custom Lovelace card — horizontal multi-calendar timeline",
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "build:watch": "rollup -c --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.1.0",
    "rollup": "^4.18.0",
    "vitest": "^1.6.0"
  }
}
